name: ODOP Quality Gate

on:
  pull_request:
    branches: [main, master,production]

jobs:
  validate-code:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12' # Odoo 19.0 target
      - name: Install
        run: pip install -r requirements.txt
      - name: Lint
        run: |
          ruff check .
          ruff format --check .
          pylint --rcfile=.pylintrc kawiil_financing
