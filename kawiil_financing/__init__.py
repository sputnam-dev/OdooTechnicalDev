# This file is what makes Python treat kawiil_financing as a package, and
# without a package Odoo cannot load the module at all. It holds no code yet.
#
# TODO (assignment 2.02): import the models folder, so Odoo picks up the model
# you defined inside it:
#     from . import models
